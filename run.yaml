device: cuda
tag: century

net:
  num_layers: 8
  dim: 512

train:
  gradient_epochs: 1
  lr: 0.0001
  batch_size: 32
  show_every: 10

self_play:
  num_games: 16
  max_len: 200

pit:
  num_games: 64
  max_len: 160
  every: 10
